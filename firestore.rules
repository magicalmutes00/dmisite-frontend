rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read site data (public website)
    match /siteData/{document} {
      allow read: true;
      // Only authenticated users can write (admin)
      allow write: if request.auth != null;
    }
  }
}
